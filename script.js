let h1  = document.querySelector('h1')
let h2  = document.querySelector('h2')
let typed  = new Typed(h1,{
    strings:["Text Gradients"],
    typedSpeed:50,
    backSpeed:25,
    loop:true
})
let typed1  = new Typed(h2,{
    strings:["Make your headlines stand out"],
    typedSpeed:50,
    backSpeed:25,
    loop:true
})